// Shared constant for the staff "Simple view" preference. Deliberately free of
// next/headers (and any server-only code) so it is safe to import from both
// server modules (staff-prefs) and client components (SimpleViewToggle).
export const SIMPLE_VIEW_COOKIE = 'va_staff_simple'
